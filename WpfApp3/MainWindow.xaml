<Window x:Class="WpfApp3.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp3"
         xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        mc:Ignorable="d"
        Title="zerzre" Height="450" Width="800">
    <Window.DataContext>
        <local:MainViewModel />
    </Window.DataContext>
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </Window.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- Source Matrix -->
        <ItemsControl Grid.Column="0" ItemsSource="{Binding SourceMatrix.Cells}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Rows="{Binding SourceMatrix.Rows}" Columns="{Binding SourceMatrix.Columns}" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Button Command="{Binding ToggleCommand}" 
                        Background="{Binding Color}" 
                        Margin="3"
                        >
                        <TextBlock TextAlignment="Center">
                            <Run Text="{Binding Label}" />
                            <LineBreak/>
                            <Run Text="{Binding ElapsedText}" />
                        </TextBlock>
                    </Button>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
        

        <!-- Overlay Scan -->
        <Border Background="#80000000" Visibility="{Binding IsScanning, Converter={StaticResource BooleanToVisibilityConverter}}" 
                Grid.ColumnSpan="2">
            <local:ScanPieceControl DataContext="{Binding ScanViewModel}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Border>
    </Grid>
</Window>
